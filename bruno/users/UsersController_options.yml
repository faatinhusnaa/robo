info:
  name: UsersController_options
  type: http
  seq: 6
  tags:
    - users

http:
  method: OPTIONS
  url: "{{baseUrl}}/users/:id"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/users/:id"
      method: OPTIONS
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
